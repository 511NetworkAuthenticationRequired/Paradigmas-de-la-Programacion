# MENSAJES BINARIOS

## a.
```smalltalk
'hola', ' aquí estoy'
```

**Imprime:** `'hola aquí estoy`

El operador `,` concatena colecciones. Como los strings en Pharo son colecciones de caracteres, `'hola'` y `' aquí estoy'` se **unen** en un único string.  

> [!IMPORTANT]
> Los strings separados por una `,`, los imprime "seguidos", por eso mismo si el segundo string NO hubiera empezado por un espacio en blanco (` `), se hubiera impreso `holaaqui estoy`.

En Pharo, los strings son objetos que representan secuencias de caracteres y pertenecen a diferentes clases según el tipo de codificación que usan. 
La clase **ByteString** se utiliza para cadenas de texto que contienen caracteres simples, generalmente del conjunto ASCII o extensiones similares, y cada carácter se almacena en un byte. Por otro lado, la clase **WideString** permite representar cadenas con caracteres Unicode, incluyendo acentos, símbolos especiales y emojis, y cada carácter puede ocupar más de un byte. Cuando concatenamos strings con el operador coma, el tipo de string resultante sigue siendo el mismo que el de los literales utilizados; por ejemplo, la concatenación de dos ByteStrings produce otro ByteString, mientras que la concatenación de WideStrings produce un WideString.

> [!TIP]
> Para distinguir entre los tipos de string, se puede enviar el mensaje class al objeto; por ejemplo, `'hola', ' aquí estoy'` class devuelve ByteString, mientras que `'hola 😊', ' aquí estoy'` class devuelve WideString, porque contiene caracteres Unicode. De esta manera, consultando `.class` se puede ver fácilmente si un string es un ByteString (solo caracteres ASCII) o un WideString (Unicode, acentos, emojis, etc.).

----

## b.
```smalltalk
#(1 2 3), #(4 5 6)
```

**Devuelve:** `#(1 2 3 4 5 6)`

El operador `,` puede usarse para concatenar dos o más arrays en uno solo; por ejemplo, `#(1 2 3), #(4 5 6)` produce `#(1 2 3 4 5 6)`. Cada array literal es un objeto de la clase Array, y el resultado de la concatenación también es un Array. Para verificar esto, se puede enviar el mensaje class: ` #(1 2 3), #(4 5 6) `, que devuelve, efectivamente, `Array`.

----

## c.
```smalltalk
4 = 5
```

**Devuelve:** `false`

La expresión `n = m` evalúa una condición de igualdad. El operador `=` pregunta si el objeto de la izquierda es igual al objeto de la derecha según la definición de igualdad para ese tipo de objeto.

En este caso:

- `n = 4` y `m = 5` son números enteros (Integer).
- Como 4 no es igual a 5, la expresión devuelve `false`.
- Es importante destacar que este operador (`=`= no asigna valores, solo compara. Para asignar un valor a una variable se usa `:=` (como en pascal).
  
# d.
```Smalltalk
#(1 2 $a), #($b 'cd')
```

**Devuelve:** `#(1 2 $a $b 'cd')`

Los arreglos pueden contener elementos de distintos tipos (arreglos heterogéneos). Por ejemplo, al concatenar `#(1 2 $a), #($b 'cd')` con el operador `,`, se obtiene un único array ` #(1 2 $a $b 'cd')` que mezcla enteros (1 y 2), caracteres (`$a` y `$b`) y un string (`'cd'`). La clase del array sigue siendo Array, pero sus elementos internos pueden ser de cualquier clase. Esto muestra que en Pharo los arrays son colecciones generales capaces de contener cualquier objeto, y que la concatenación une sus elementos respetando el orden original, sin importar su tipo.
